name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Lint and syntax check
        run: |
          python -m py_compile Remind2Rest.py notifications.py tray_icon.py web_configurator.py || true

      - name: Smoke import
        run: |
          python - <<PY
          import importlib.util, sys
          for f in ["Remind2Rest.py", "notifications.py", "tray_icon.py", "web_configurator.py"]:
              spec = importlib.util.spec_from_file_location("mod", f)
              mod = importlib.util.module_from_spec(spec)
              try:
                  spec.loader.exec_module(mod)
                  print(f"Imported {f} OK")
              except Exception as e:
                  print(f"Import {f} failed: {e}")
                  sys.exit(1)
          PY
